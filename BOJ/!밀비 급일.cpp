#include <bits/stdc++.h>
#include <unsitd.h>

using namespace std;

int main(){
    char a[501] = {0,};
    int b = read(0, a, 500);
    a[b] = 0;
    int i = 0;
    while (1){
        int j = i + 1;
        string c;
        while (a[j] && a[j] != '\n')
            j++;
        
    }
}