#include <bits/stdc++.h>

#define REP(i, a, b) for (int i = a; i <= b; i++)

using namespace std;

int main(){
	string s;
	cin >> s;
	vector<int> v;
	int sum = 0;
	for (int i = 0; i < (int)s.size(); i++){
		if (s[i] == '('){

		}else if (s[i] == ')'){

		}else{
			
		}
	}

	return 0;
}