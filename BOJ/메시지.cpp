#include <iostream>
#include <cassert>
#include <string>

using namespace std;

int main() {
  for (int i = 1; i <= 2; i++){
    int n;
    cin >> n;

    string  names[51];
    char    reply[51][51];
    memset(reply, 0, sizeof(reply));

    for (int j = 1; j <= n; j++) {
      cin >> names[j];
      
    }
  }


}