#include <string>
#include <vector>
#include <utility>

using namespace std;

vector<int> solution(vector<int> numbers) {
    vector<int> answer[(int)numbers.size()];
    
    vector<pair<int, int>> arr;
    

    //현재 num 과 arr 비교.

    //num보다 작으면 해당 인덱스 초기화
    for (int i = 0; i < numbers.size(); i++)
    {
        for (auto x : arr)
        {
            if (x.first )
        }
    }
    
    return answer;
} 